# Индекс документации

## Исходные материалы

docs/wishes.md — пожелания владельца в свободной форме. Это вход для подготовки ТЗ, а не утверждённая спецификация. Валидатор не требует оформлять пожелания как нормы.

## Карта нормативных документов

Документов ТЗ пока нет. Агент добавляет сюда их адреса и назначение по мере утверждения и одновременно включает файлы в corpus в governance.json.

## Реестр

| ID | Title | Canonical | Status |
|---|---|---|---|

## Решения и связи

- 2026-09-25. Переход ТЗ v1.5 -> v1.6 по документу «Docs/Корзина/apply-ready patch set для перехода с v1.5 на v1.6.md». Согласован вариант 2 применения блоков §1: текст вставляется дословно, восстанавливается только markdown-разметка заголовков (N. -> уровень 2, N.M. -> уровень 3, N.M.K. -> уровень 4); якоря и старые фрагменты ищутся после нормализации (снятие акцентов, backtick и ведущих решёток, схлопывание пробелов); операция «новый пункт» вставляет строки списка с префиксом дефиса. Строки с отступом (шаги алгоритма вида 5.1.) и односоставные номера внутри списков заголовками не считаются.
- 2026-09-25. ТЗ v1.5 не изменяется: результат пишется в копию, созданную из ТЗ v1.5 (на момент записи — «Docs/MiniMem-1 - Техническое задание v1.6.md»; действующий адрес см. в карте документов). Применение выполняется вспомогательным скриптом вне репозитория; запись в ТЗ — только после устранения расхождений из раздела «Известные расхождения».
- 2026-09-25. Правило 10 patch set отложено: реестр доработок «Docs/MiniMem-1 - Реестр доработок после эксплуатации.md» уже существует, создание не требуется.
- 2026-09-25. Правило 8 patch set снято с отсрочки по команде владельца: создан каталог `tests/` и файл `tests/required_tests.txt` — номера 1–115, по одному в строке, LF, UTF-8, без пустых строк и дублей. Список соответствует разделу 23 ТЗ v1.6: в нём 115 тестов со статусом «действует», пропусков и повторов нет, поэтому обязательными являются все номера 1–115. Проверка соответствия выполняется выводом номеров из §23; тесты и `tests/test_required_numbers.py` появятся позже.
- 2026-09-26. По команде владельца три документа перемещены из `Docs/` в `Docs/Корзина/`: «apply-ready patch set для перехода с v1.5 на v1.6.md», «вопросы к ТЗ.md», «MiniMem-1 - Техническое задание v1.5.md». Причина: это материалы, отработавшие переход v1.5 -> v1.6; действующие редакции — «Docs/MiniMem-1 - Техническое задание v1.6.md» (на момент записи; ныне «Docs/MiniMem-1 - Техническое задание v1.7.md») и «Docs/MiniMem-1 - План реализации.md». Каталог `Docs/Корзина/` не ведётся в репозитории (`.gitignore`), поэтому из индекса Git файлы удалены, копии остались на диске; ссылка на patch set в этом разделе обновлена.
- 2026-09-26. Финальная чистка текста: в «Docs/MiniMem-1 - План реализации.md» заменено 6 вхождений «записии» на «записи» (строки 141, 196, 239, 253, 285, 389). В ТЗ v1.6 опечаток «записии»/«залоировать» нет. Известная оставшаяся опечатка: в «Docs/MiniMem-1 - Реестр доработок после эксплуатации.md» строки 14, 22, 34 содержат «записии».
- 2026-09-26. Опечатка «записии» устранена по всему репозиторию: в «Docs/MiniMem-1 - Реестр доработок после эксплуатации.md» заменено 2 вхождения (строки 14, 34; строка 22 исправлена ранее при правке файла владельцем). Совпадений по «записии» и «залоировать» в отслеживаемых файлах не осталось.
- 2026-09-26. По команде владельца хук `post_llm.py` зарегистрирован в Hermes досрочно (этап 1 плана, вне этапа 8), чтобы выполнять требование «с этого этапа журнал начинает накапливать реальные записи». Причина: без регистрации журнал остаётся пустым. Изменения вне репозитория: в `%LOCALAPPDATA%\hermes\config.yaml` добавлен блок `hooks.post_llm_call` (command = hermes-venv python + `C:\Projects\MEM_Hermes\minimem\hooks\post_llm.py`, timeout 30); в `shell-hooks-allowlist.json` добавлена соответствующая запись approvals. Резервные копии обоих файлов: `config.yaml.minimem-20260926-032106.bak` и `shell-hooks-allowlist.json.minimem-20260926-032106.bak`. Проверка: `hermes hooks list` показывает 2 хука, `hermes hooks doctor` — «All shell hooks look healthy», оба allowlisted; пробная отправка события post_llm_call создала запись в `C:\Projects_loc\LLM_Wiki\memory\common\memory\2026-09-26.md` и `minimem.db`; `minimem status` показывает 1 файл журнала (622 байта) и SQLite `ok`. Хуки `pre_llm.py` и `session_end.py` не созданы и не регистрировались: они появляются на этапах 4 и 3/7. Ежедневный бэкап журнала не настроен — отдельная задача, процедура описана в ещё не созданном документе «Установка, бэкап и восстановление». Замечание: `project_mapping` в `minimem/config.json` пуст, поэтому ходы из `C:\Projects\MEM_Hermes` пишутся в проект `common`; при необходимости маппинг заполняется на этапе 8 при установке.
- 2026-09-26. Выполнен этап 1 плана реализации v1.7 (Захват и журнал) по команде владельца. Созданы: `minimem/mm/redaction.py` (встроенные линейные шаблоны §7.1, детерминированные placeholder'ы, `redaction_set`, таймаут по `redaction_timeout_ms`), `minimem/mm/canonical.py` (канонизация §5, `content_hash` до обрезки, `event_id`, сериализация и экранирование тела §4.4), `minimem/mm/journal.py` (атомарный append под `msvcrt.locking` с разделительной пустой строкой, сверка размера и завершающего маркера §4.5.1, разбор конечным автоматом §4.5.3 с байтовыми смещениями), `minimem/mm/store.py` (минимальный слой: `meta`, `capture_guard`, WAL, busy_timeout 5000), `minimem/mm/insert.py` (делимитеры §13, `sanitize_for_insert` §13.1, `strip_memory_block` — удаление ровно одного блока памяти из реплики), `minimem/mm/capture.py` (конвейер Захвата по §3.1 и §10: очистка, redaction, хеш, обрезка, `max_memory_record`, идемпотентность и ревизии §5.1, гард best-effort), `minimem/hooks/post_llm.py` (тонкий хук, всегда код 0, `MINIMEM_CONFIG` для тестов), `minimem/tests/test_capture.py`. Прогон: 94 теста (40 этапа 0 + 54 этапа 1), OK, код 0. Реализованы номера ТЗ 1-8, 13, 14, MM-51, MM-61-MM-69, MM-85, MM-86, MM-89, MM-100-MM-102, MM-105-MM-107, MM-116-MM-118, MM-134-MM-138, MM-148, MM-149, MM-162 и инфраструктурные проверки. Не реализовано и остаётся на этап 2: FTS5-индекс, курсоры, `rebuild-index`, `show`, проверки `verify` по соответствию журнала и индекса. Замечания: (1) добавлен модуль `mm/capture.py` — в перечне файлов плана (п.2) его нет, но §3.0 требует модуль Захвата, а §26.2 — «вся логика в модулях, не в скриптах хуков»; (2) незакрытый блок памяти в реплике не вырезается (ломаная разметка лучше потери текста реплики), фиксируется как `malformed`; (3) недопустимое значение закрытого перечисления даёт ошибку метаданных записи, а не journal_record_damaged (§4.4 п.3, MM-100); (4) если одних метаданных больше `max_memory_record`, тело урезается полностью, а запись превышает лимит — метаданные не отбрасываются (§6 п.3); (5) ежедневный бэкап журнала по плану запускается с этого этапа, но процедура бэкапа описана в документе «Установка, бэкап и восстановление», который ещё не создан — до этапа 8 бэкап не настроен.
- 2026-09-26. Выполнен этап 0 плана реализации v1.7 (каркас) по команде владельца. Созданы: `minimem/config.json` (значения раздела 20 ТЗ в редакции v1.7), `minimem/mm/config.py` (умолчания, проверка типов, инварианты §20 п.3, `config_hash` и `project_mapping_hash` по П-14), `minimem/mm/paths.py` (структура §4.1, часовой пояс `journal_timezone`, безопасные имена файлов), `minimem/mm/log.py` (структурированный лог §22/§19.5, запрет полей с пользовательским текстом, секретами и traceback по §7.1), `minimem/mm/project.py` (определение проекта по §8 с Windows-нормализацией и самым длинным префиксом), `minimem/minimem.py` (каркас CLI с командами `status` и `verify`, `--config`), `minimem/tests/test_stage0_core.py` (40 инфраструктурных тестов этапа 0). Прогон: 40 тестов, OK, код возврата 0 (Python 3.11.16 из `hermes-agent\venv`). Номера MM-NN тестам этапа 0 не присвоены: план относит их к инфраструктурным, обязательных приёмочных номеров на этом этапе нет; `tests/required_tests.txt` (142 номера) не изменялся. Проверки журнала и индекса, курсоры, схема SQLite и остальные команды CLI появляются на этапе 2; в `verify` это отмечено явно.
- 2026-09-26. Добавлен документ «Docs/MiniMem-1 - План правок ТЗ v1.6.md» (536 строк) — план правок ТЗ по результатам архитектурного разбора ТЗ v1.6. Правки П-01…П-16 (часть помечена «блокирует релиз»), развития Р-NN вне рамок v1.6, ссылки на номера тестов MM-NN. Функционал приложения не расширяется. Документ внесён в репозиторий, в нормативный корпус не входит.
- 2026-09-26. Добавлены два документа: «Docs/MiniMem-1_Reshenia_i_plan_pravok_v1.7Sonnet.md» (245 строк) и «Docs/MiniMem-1_Решения_по_вопросам_и_план_правок_ТЗ_v1.6DS.md» (459 строк). Оба описывают решения по развилкам плана правок и план внесения правок в ТЗ с выходом на редакцию v1.7 (пункты П-NN). В репозиторий внесены как рабочие материалы, в нормативный корпус не входят и правок в ТЗ v1.6 пока не требуют.
- 2026-09-26. По команде владельца из `Docs/` перенесены в `Docs/Корзина/` четыре документа: «MiniMem-1 - План правок ТЗ v1.6.md», «MiniMem-1_Reshenia_i_plan_pravok_v1.7Sonnet.md», «MiniMem-1_Решения_по_вопросам_и_план_правок_ТЗ_v1.6DS.md», «MiniMem-1_v1.7_Таблица_соответствия_вопросов.md». Причина: правки уже внесены в ТЗ (редакция v1.7), планы отработали. Каталог `Docs/Корзина/` не ведётся в репозитории, поэтому файлы удалены из индекса Git, копии остались на диске. Ссылка на план правок в заголовках ТЗ v1.6, плана реализации и changelog_tz.md сохранена как историческая: она указывает на исходный документ, а не на его текущее расположение.

## Известные расхождения

- 2026-09-25 · закрыто · patch set §1 против ТЗ v1.5 · потеря fence-обёрток. Нормативные блоки в §1 оформлены как text-fence, поэтому внутренние кодовые блоки были записаны простым текстом. По команде владельца от 2026-09-25 fence-обёртки возвращены в ТЗ v1.6: §4.4 (образец записи журнала, ```text), §9.1 (четыре блока ```sql + перечни значений meta и digest_status в ```text), §13 (пример блока памяти, ```text) и §13.1 (строки-делимитеры, fence с отступом внутри пункта 1), §14 (путь к файлу и образец дайджеста, ```text), §18 (три схемы жизненного цикла и параметры catch-up, ```text), §20.1 (перечень флагов, ```text), §21 (список команд, ```text), §22 (поля лога и значения operation, ```text). Нормативные списки (маркированные и нумерованные требования) намеренно оставлены обычным markdown. Подраздел 17.1 не добавляется по решению владельца.
- 2026-09-25 · контроль 6.3 · подраздел 17.1 в patch set отсутствует: в блоке пункта 1.29 подразделов 17.x нет. По решению владельца от 2026-09-25 17.1 не добавляется — раздел 17 в v1.6 заменён целиком и описывает состояние сессии. Расхождение закрыто.
- 2026-09-25 · patch set §1 · опечатка «записии» вместо «записи» встречается в блоках пунктов 1.14, 1.15, 137.2, 137.3. По решению владельца опечатка исправлена по смыслу при применении; в ТЗ v1.6 эти правки также исправлены по всему документу вместе с «залоировать» -> «залогировать», «некзакчающиеся строки» -> «изменённые строки, не совпадающие с оригинальными делимитерами», «поисковое term» -> «поисковый термин».
- 2026-09-25 · patch set §1 · подзаголовки, не содержащие номера (например «Первый ход сессии», «Каждый ход» в разделе 18 и имена команд CLI в разделе 21), в блоках записаны простым текстом; при варианте 2 разметка таких строк не восстанавливается.
- 2026-09-25 · закрыто · пункт 137.2, тест 50: опечатка «записии» признана опечаткой patch set по решению владельца, фрагмент найден по тексту ТЗ v1.5 («записи»), правка применена.
- 2026-09-25 · закрыто · пункт 1.15: граница раздела 4.4 восстановлена, образец записи журнала возвращён внутрь fence, раздел 4.5 стоит после полного 4.4.
- 2026-09-25 · закрыто · раздел 18.1 «Catch-up»: порядок заменён на «сначала уплотнение (mode_compaction = true), затем дайджест (mode_return = true)»; при потере состояния сессий после rebuild-index автоматический catch-up не выполняет ни уплотнение, ни создание дайджеста.
- 2026-09-25 · выполнено · §2 patch set: «Docs/MiniMem-1 - План реализации.md» заменён на версию v1.6 (269 -> 404 строки) по команде владельца от 2026-09-25. Прежняя редакция плана сохранена вне репозитория во временном каталоге.
- 2026-09-25 · закрыто · «Docs/changelog_tz.md»: файл создан, вводный блок истории правок вынесен из ТЗ v1.6, в ТЗ оставлена краткая ссылка.
- 2026-09-25 · устранено · тест 50 дополнен проверкой «количество терминов не превышает max_search_terms»; тест 3 — убраны лишние backtick вокруг `event_id`; новые тесты 51-115 получили явные номера MM-51…MM-115.
- 2026-09-26 · открыто · `tests/required_tests.txt` не соответствует редакции v1.7. Раздел 23 ТЗ v1.6 (в редакции v1.7) содержит 164 строки тестов, номера 1–164 без пропусков и повторов; из них MM-112 помечен «снят (дубль MM-96)». Раздел «Обязательные номера» задаёт состав: 1–50, 51–68, 79–89, 101–115 и все MM-116…MM-164, кроме снятых номеров; 69–78 и 90–100 имеют статус «действует, для первой приёмки не обязателен». Ожидаемый файл — 142 номера. Фактический файл содержал 115 номеров (1–115) и устарел: отсутствовали 116–164 (49 номеров), лишние 69–78, 90–100 (21 номер) и снятый 112.
- 2026-09-26 · закрыто · `tests/required_tests.txt` пересобран по §23 (редакция v1.7): 142 номера — 1–50, 51–68, 79–89, 101–164 без снятого MM-112. Формат: по одному номеру в строке, LF (CR-байтов 0), UTF-8 без BOM, пустых строк и дублей нет, диапазон 1…164. Номера сверены с §23: все присутствуют, лишних нет. Проверка соответствия описана в §23 и критериях приёмки (MM-153, критерии 11 и 25) — тест `tests/test_required_numbers.py` ещё не создан.
- 2026-09-26. По решению владельца документ переименован: `Docs/MiniMem-1 - Техническое задание v1.6.md` -> `Docs/MiniMem-1 - Техническое задание v1.7.md`. Причина: содержимое — редакция v1.7, а имя файла оставалось от предыдущей редакции, из-за чего адрес документа расходился с его содержимым. Правило: имя файла ТЗ совпадает с номером редакции внутри; при выпуске следующей редакции файл переименовывается, исторические упоминания старого имени в журнале решений и changelog остаются как есть. Обновлены рабочие ссылки: `Docs/MiniMem-1 - План реализации.md` (строка 3) и запись от 2026-09-25 в этом файле. Внутри ТЗ (строка 3) и в `Docs/changelog_tz.md` имя не дублируется — там только номер редакции.
