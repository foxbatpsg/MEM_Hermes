# Индекс документации

## Исходные материалы

docs/wishes.md — пожелания владельца в свободной форме. Это вход для подготовки ТЗ, а не утверждённая спецификация. Валидатор не требует оформлять пожелания как нормы.

## Карта нормативных документов

Документов ТЗ пока нет. Агент добавляет сюда их адреса и назначение по мере утверждения и одновременно включает файлы в corpus в governance.json.

## Реестр

| ID | Title | Canonical | Status |
|---|---|---|---|

## Решения и связи

- 2026-09-25. Переход ТЗ v1.5 -> v1.6 по документу «Docs/Корзина/apply-ready patch set для перехода с v1.5 на v1.6.md». Согласован вариант 2 применения блоков §1: текст вставляется дословно, восстанавливается только markdown-разметка заголовков (N. -> уровень 2, N.M. -> уровень 3, N.M.K. -> уровень 4); якоря и старые фрагменты ищутся после нормализации (снятие акцентов, backtick и ведущих решёток, схлопывание пробелов); операция «новый пункт» вставляет строки списка с префиксом дефиса. Строки с отступом (шаги алгоритма вида 5.1.) и односоставные номера внутри списков заголовками не считаются.
- 2026-09-25. ТЗ v1.5 не изменяется: результат пишется в копию, созданную из ТЗ v1.5 (на момент записи — «Docs/MiniMem-1 - Техническое задание v1.6.md»; действующий адрес см. в карте документов). Применение выполняется вспомогательным скриптом вне репозитория; запись в ТЗ — только после устранения расхождений из раздела «Известные расхождения».
- 2026-09-25. Правило 10 patch set отложено: реестр доработок «Docs/MiniMem-1 - Реестр доработок после эксплуатации.md» уже существует, создание не требуется.
- 2026-09-25. Правило 8 patch set снято с отсрочки по команде владельца: создан каталог `tests/` и файл `tests/required_tests.txt` — номера 1–115, по одному в строке, LF, UTF-8, без пустых строк и дублей. Список соответствует разделу 23 ТЗ v1.6: в нём 115 тестов со статусом «действует», пропусков и повторов нет, поэтому обязательными являются все номера 1–115. Проверка соответствия выполняется выводом номеров из §23; тесты и `tests/test_required_numbers.py` появятся позже.
- 2026-09-26. По команде владельца три документа перемещены из `Docs/` в `Docs/Корзина/`: «apply-ready patch set для перехода с v1.5 на v1.6.md», «вопросы к ТЗ.md», «MiniMem-1 - Техническое задание v1.5.md». Причина: это материалы, отработавшие переход v1.5 -> v1.6; действующие редакции — «Docs/MiniMem-1 - Техническое задание v1.6.md» (на момент записи; ныне «Docs/MiniMem-1 - Техническое задание v1.7.md») и «Docs/MiniMem-1 - План реализации.md». Каталог `Docs/Корзина/` не ведётся в репозитории (`.gitignore`), поэтому из индекса Git файлы удалены, копии остались на диске; ссылка на patch set в этом разделе обновлена.
- 2026-09-26. Финальная чистка текста: в «Docs/MiniMem-1 - План реализации.md» заменено 6 вхождений «записии» на «записи» (строки 141, 196, 239, 253, 285, 389). В ТЗ v1.6 опечаток «записии»/«залоировать» нет. Известная оставшаяся опечатка: в «Docs/MiniMem-1 - Реестр доработок после эксплуатации.md» строки 14, 22, 34 содержат «записии».
- 2026-09-26. Опечатка «записии» устранена по всему репозиторию: в «Docs/MiniMem-1 - Реестр доработок после эксплуатации.md» заменено 2 вхождения (строки 14, 34; строка 22 исправлена ранее при правке файла владельцем). Совпадений по «записии» и «залоировать» в отслеживаемых файлах не осталось.
- 2026-09-26. По команде владельца развёрнут ежедневный бэкап журнала по разделу 3 документа «Docs/MiniMem-1 - Установка, бэкап и восстановление.md» (вне этапа 8 плана, досрочно). Механизм и каталог — как в документе: `robocopy` с ключами `/E /R:1 /W:1 /NFL /NDL`, слепок `D:\minimem-backup\<YYYY-MM-DD>` с подкаталогами `journal` (копия `<memory_root>` = `C:\Projects_loc\LLM_Wiki\memory`), `minimem` (код с `config.json`) и `hermes\config.yaml`. Скрипт `D:\minimem-backup\minimem-backup.ps1` (вне репозитория), запуск вручную даёт ту же копию, что и задача — это и есть «копия после завершения работы с проектом» из раздела 3. Ротация: хранятся 30 последних ежедневных копий и до 12 месячных (копии за последний день месяца не удаляются). Задача Планировщика Windows `\MiniMem\Backup daily`: ежедневно в 23:00, `RunLevel=Limited`, `UserId=user`, `LogonType=Interactive`, разрешено выполнение от батареи и запуск при пропуске. Код возврата скрипта: 0 при успехе, 1 если хотя бы один `robocopy` вернул >= 8; проверено дважды — из консоли и через запуск задачи планировщиком (LastTaskResult 1 на отказе, 0 на успешном прогоне). Выбранное владельцем место: `D:\minimem-backup` (диск 4, ST6000VN001 HDD, физически не тот, что C:). Проверки: ручной прогон создал слепок `D:\minimem-backup\2026-09-26` (24 файла, 366 954 байта), все три `robocopy` вернули код 1; тренировочное восстановление по разделу 4 выполнено в `C:\Temp\minimem-restore-drill` (код и `config.json` из слепка, журнал из слепка, `memory_root` во временной копии `config.json` переведён на временный каталог, `minimem.db` удалён) — `rebuild-index` проиндексировал 8 записей (0 дублей, 0 повреждений), `verify` — «без проблем», `show e_ef1fc10f…` вернул исходную запись; рабочая система до и после: 8 записей в журнале и 8 в индексе, `verify` — «без проблем». Рабочее хранилище не изменялось: `minimem.db` и журнал имеют прежние размер и время. Замечание (в отчёте, репозиторий не менялся): `minimem/` и `minimem/config.json` уже находятся под Git, поэтому копия каталога MiniMem и `config.json` в бэкапе дублирует содержимое репозитория; назначение копии — восстановление после сбоя, а не основание для правок, при расхождении приоритет у рабочей копии. Прогон тестов: 114 тестов, OK, код 0; `validate_index.py --strict` — PASS, код 0; `build_refs_registry.py --check` — «Реестр ссылок актуален», код 0. Реестр ссылок не пересобирался: `INDEX.md` правился в разделе решений, новых ссылок на нормы не добавлено. Открытые наблюдения: (1) `robocopy` отвергает раздельную форму `/LOG <путь>` кодом 16 — в скрипте используется `/LOG:<путь>`; (2) копия `journal` включает производные файлы (`minimem.db`, `logs\minimem.log`) — документ их не исключает явно, они пересобираются по разделу 4; (3) месячные копии хранятся сверх 30 ежедневных и тоже попадают в резерв, пока их не станет больше 12.
- 2026-09-26. По команде владельца принято решение о внешней настройке резервного копирования (слой 1) и о контроле копии в `minimem doctor` (П-19). (1) Настройки вынесены из скрипта в эксплуатационный файл `D:\minimem-backup\backup.json`: `backup_root`, `minimem_dir`, `keep_daily`, `keep_monthly`; `memory_root` скрипт берёт из `minimem\config.json`. Причина: место хранения и глубина хранения менялись только правкой кода, а периодичность — только через задачу Планировщика. (2) По требованию владельца: если `backup_root` не указан или диск с ним недоступен (диск отключён, буква пропала), копирование идёт в каталог `minimem-backup` рядом с `memory_root`, с предупреждением в выводе и указанием режима (`configured`/`fallback`). Причина: смена дисков в системе не должна оставлять журнал без копий. Ограничение зафиксировано в документе эксплуатации: запасной режим не спасает при поломке диска с данными. (3) Сверка тома (слой 2, серийный номер/метка) отклонена владельцем, в реализацию не входит. (4) Решение П-19: `minimem doctor` получает проверку наличия и свежести последнего слепка и код `backup_stale`. Причина: задача Планировщика при отказе возвращает ненулевой код, но не сообщает владельцу — молчащий бэкап оставляет журнал без копий. Порог в днях в ТЗ не зафиксирован: он принадлежит эксплуатации (`backup_stale_days`); хуки не читают каталог копий и не меняют поведение при отказе. Правки: ТЗ v1.7 §21.1, §19.5, §19.1, §23 (MM-165…MM-168, включены в обязательные), §25 (критерий 29); `tests/required_tests.txt` 142 → 146; план реализации (этап 8: что делаем, критерий готовности, тесты этапа, шапка); документ «Установка, бэкап и восстановление» (раздел 3: таблица настроек, запасной каталог, коды возврата, doctor; раздел 6: регламент по смене дисков); реестр доработок RB-12 (активное оповещение об отказе остаётся кандидатом, почта и внешние сервисы исключены); `Docs/changelog_tz.md`. Номер решения — П-19, а не П-17: номер П-17 занят в ТЗ пунктом «Прерванная сессия». Файл предложения переименован в «Docs/MiniMem-1 - Предложение - контроль бэкапа в doctor.md» и переведён в статус «принято», в нормативный корпус не входит. Реализация скрипта проверена: штатный режим — код 0; недоступный `backup_root` и пустой ключ — оба дают запасной каталог и код 0; отсутствующий, битый и неполный `backup.json`, отсутствующий `config.json` MiniMem, `keep_daily=0` — код 2; неопределимый каталог копии — код 3. Задача `\MiniMem\Backup daily` после правок прогнана, `LastTaskResult=0`. Песочницы в `C:\Temp` удалены. Не выполнено: код `doctor` и тесты MM-165…MM-168 (этап 8), watchdog/уведомления (RB-12), коммит и пуш — по отдельной команде.
- 2026-09-26. Вопрос о периодичности и событийности резервного копирования вынесен в реестр доработок как RB-13 и RB-14, по решению владельца («запиши всё в доработки на будущее»), правок в ТЗ не вносить. Зафиксировано: (1) в ТЗ периодичность и события копирования не определены вовсе, а фраза «плюс копия после завершения работы с проектом» из документа «Установка, бэкап и восстановление» не расшифрована — что считается завершением, кто выполняет, что при пропуске; (2) довод владельца: календарная копия бессмысленна без изменений, журнал только дописывается, копия нужна по изменению, а изменение приходит через ход; (3) предложение к будущей правке ТЗ: завершение работы = конец сессии, копирование по изменению, календарь как страховка, два пути независимы; (4) `hermes cron` как замена Планировщику Windows пока заблокирован: шлюз не запущен, последний тик 3 ч 40 мин назад, задач нет; кроме того `--script` принимает только `.sh`/`.bash` и Python, текущий скрипт PowerShell — нужна обёртка. Обе записи имеют статус «Кандидат», решения не приняты, код и документация не менялись. План: RB-13 при согласовании даст номер П-20 и затронет ТЗ, документ эксплуатации и план. Изменения: `minimem/mm/store.py` — полная схема §9.1 (`memory_fts` FTS5/unicode61, `memory_meta` с полями `turn_numeric`/`revision`/`fmt` и тремя обязательными индексами, `usage_counters`, `suppressed_records`, `sessions`, `session_returns`, `journal_cursors`, `meta`), `schema_version` поднят до 2, добавлены методы работы с индексом, курсорами, снятием и счётчиками, `drop_index_layer()`, `schema_version()`, отметки о повреждённых записях `damaged:*` в `meta` (П-13); `minimem/mm/indexer.py` — `index_record` с решением по event_id/content_hash/revision, `_resolve_offset` со сбросом курсора при уменьшении файла или выходе смещения за размер, `_read_complete_records` (чтение только от курсора, неполная запись не продвигает курсор), `index_journal_file`, `index_project` с лимитом прохода 500 записей, `rebuild_index`, `verify` (записи вне индекса и вне журнала, дубликаты, повреждения, курсоры, несоответствие content_hash, несколько ревизий, производные turn); `minimem/minimem.py` — команды `rebuild-index` и `show`, `verify` расширен проверками индекса; `minimem/hooks/post_llm.py` — запуск Индексатора отдельным проходом после Захвата (§2: Индексатор читает журнал, данные Захвата не получает); `minimem/tests/test_indexer.py` — 20 тестов. Прогон: 114 тестов (40 этапа 0 + 54 этапа 1 + 20 этапа 2), OK, код 0. Реализованы номера ТЗ 9-12, 15, 16, 43, MM-57, MM-59, MM-64, MM-70-MM-73, MM-103-MM-105, MM-115, MM-149, MM-162, MM-163. Проверка на реальных данных: `minimem rebuild-index` проиндексировал 7 записей, `minimem verify` — «без проблем», повторный прогон идемпотентен; пробный запуск хука записал и проиндексировал новую запись одним проходом (`records_indexed: 1`). Найденная и исправленная ошибка этапа: в DDL `memory_fts` отсутствовало ключевое слово `VIRTUAL` (`CREATE TABLE … USING fts5`), из-за чего создание схемы падало; для диагностики добавлен `minimem/tools/check_fts5.py`. Замечания: (1) `journal_offset` в `memory_meta` — байтовое смещение начала записи в файле журнала, при усечении или внешнем переписывании файла `verify` сообщает расхождение смещения, а индекс не удаляется; (2) отметки о повреждённых записях хранятся в `meta` по ключу `damaged:<файл>@<смещение>@<mtime>` — повторные обнаружения той же связки не пишут новых строк лога, но увеличивают счётчик `records_damaged` в `minimem stats` (этап 7); (3) `sessions` и `session_returns` созданы по схеме §9.1, но не заполняются — состояние сессий появляется на этапе 7; (4) уплотнение и снятие записей не реализованы, таблицы `suppressed_records` и `usage_counters` пока пусты (этап 6).
- 2026-09-26. По команде владельца хук `post_llm.py` зарегистрирован в Hermes досрочно (этап 1 плана, вне этапа 8), чтобы выполнять требование «с этого этапа журнал начинает накапливать реальные записи». Причина: без регистрации журнал остаётся пустым. Изменения вне репозитория: в `%LOCALAPPDATA%\hermes\config.yaml` добавлен блок `hooks.post_llm_call` (command = hermes-venv python + `C:\Projects\MEM_Hermes\minimem\hooks\post_llm.py`, timeout 30); в `shell-hooks-allowlist.json` добавлена соответствующая запись approvals. Резервные копии обоих файлов: `config.yaml.minimem-20260926-032106.bak` и `shell-hooks-allowlist.json.minimem-20260926-032106.bak`. Проверка: `hermes hooks list` показывает 2 хука, `hermes hooks doctor` — «All shell hooks look healthy», оба allowlisted; пробная отправка события post_llm_call создала запись в `C:\Projects_loc\LLM_Wiki\memory\common\memory\2026-09-26.md` и `minimem.db`; `minimem status` показывает 1 файл журнала (622 байта) и SQLite `ok`. Хуки `pre_llm.py` и `session_end.py` не созданы и не регистрировались: они появляются на этапах 4 и 3/7. Ежедневный бэкап журнала не настроен — отдельная задача, процедура описана в ещё не созданном документе «Установка, бэкап и восстановление». Замечание: `project_mapping` в `minimem/config.json` пуст, поэтому ходы из `C:\Projects\MEM_Hermes` пишутся в проект `common`; при необходимости маппинг заполняется на этапе 8 при установке.
- 2026-09-26. Выполнен этап 1 плана реализации v1.7 (Захват и журнал) по команде владельца. Созданы: `minimem/mm/redaction.py` (встроенные линейные шаблоны §7.1, детерминированные placeholder'ы, `redaction_set`, таймаут по `redaction_timeout_ms`), `minimem/mm/canonical.py` (канонизация §5, `content_hash` до обрезки, `event_id`, сериализация и экранирование тела §4.4), `minimem/mm/journal.py` (атомарный append под `msvcrt.locking` с разделительной пустой строкой, сверка размера и завершающего маркера §4.5.1, разбор конечным автоматом §4.5.3 с байтовыми смещениями), `minimem/mm/store.py` (минимальный слой: `meta`, `capture_guard`, WAL, busy_timeout 5000), `minimem/mm/insert.py` (делимитеры §13, `sanitize_for_insert` §13.1, `strip_memory_block` — удаление ровно одного блока памяти из реплики), `minimem/mm/capture.py` (конвейер Захвата по §3.1 и §10: очистка, redaction, хеш, обрезка, `max_memory_record`, идемпотентность и ревизии §5.1, гард best-effort), `minimem/hooks/post_llm.py` (тонкий хук, всегда код 0, `MINIMEM_CONFIG` для тестов), `minimem/tests/test_capture.py`. Прогон: 94 теста (40 этапа 0 + 54 этапа 1), OK, код 0. Реализованы номера ТЗ 1-8, 13, 14, MM-51, MM-61-MM-69, MM-85, MM-86, MM-89, MM-100-MM-102, MM-105-MM-107, MM-116-MM-118, MM-134-MM-138, MM-148, MM-149, MM-162 и инфраструктурные проверки. Не реализовано и остаётся на этап 2: FTS5-индекс, курсоры, `rebuild-index`, `show`, проверки `verify` по соответствию журнала и индекса. Замечания: (1) добавлен модуль `mm/capture.py` — в перечне файлов плана (п.2) его нет, но §3.0 требует модуль Захвата, а §26.2 — «вся логика в модулях, не в скриптах хуков»; (2) незакрытый блок памяти в реплике не вырезается (ломаная разметка лучше потери текста реплики), фиксируется как `malformed`; (3) недопустимое значение закрытого перечисления даёт ошибку метаданных записи, а не journal_record_damaged (§4.4 п.3, MM-100); (4) если одних метаданных больше `max_memory_record`, тело урезается полностью, а запись превышает лимит — метаданные не отбрасываются (§6 п.3); (5) ежедневный бэкап журнала по плану запускается с этого этапа, но процедура бэкапа описана в документе «Установка, бэкап и восстановление», который ещё не создан — до этапа 8 бэкап не настроен.
- 2026-09-26. Выполнен этап 0 плана реализации v1.7 (каркас) по команде владельца. Созданы: `minimem/config.json` (значения раздела 20 ТЗ в редакции v1.7), `minimem/mm/config.py` (умолчания, проверка типов, инварианты §20 п.3, `config_hash` и `project_mapping_hash` по П-14), `minimem/mm/paths.py` (структура §4.1, часовой пояс `journal_timezone`, безопасные имена файлов), `minimem/mm/log.py` (структурированный лог §22/§19.5, запрет полей с пользовательским текстом, секретами и traceback по §7.1), `minimem/mm/project.py` (определение проекта по §8 с Windows-нормализацией и самым длинным префиксом), `minimem/minimem.py` (каркас CLI с командами `status` и `verify`, `--config`), `minimem/tests/test_stage0_core.py` (40 инфраструктурных тестов этапа 0). Прогон: 40 тестов, OK, код возврата 0 (Python 3.11.16 из `hermes-agent\venv`). Номера MM-NN тестам этапа 0 не присвоены: план относит их к инфраструктурным, обязательных приёмочных номеров на этом этапе нет; `tests/required_tests.txt` (142 номера) не изменялся. Проверки журнала и индекса, курсоры, схема SQLite и остальные команды CLI появляются на этапе 2; в `verify` это отмечено явно.
- 2026-09-26. Добавлен документ «Docs/MiniMem-1 - План правок ТЗ v1.6.md» (536 строк) — план правок ТЗ по результатам архитектурного разбора ТЗ v1.6. Правки П-01…П-16 (часть помечена «блокирует релиз»), развития Р-NN вне рамок v1.6, ссылки на номера тестов MM-NN. Функционал приложения не расширяется. Документ внесён в репозиторий, в нормативный корпус не входит.
- 2026-09-26. Добавлены два документа: «Docs/MiniMem-1_Reshenia_i_plan_pravok_v1.7Sonnet.md» (245 строк) и «Docs/MiniMem-1_Решения_по_вопросам_и_план_правок_ТЗ_v1.6DS.md» (459 строк). Оба описывают решения по развилкам плана правок и план внесения правок в ТЗ с выходом на редакцию v1.7 (пункты П-NN). В репозиторий внесены как рабочие материалы, в нормативный корпус не входят и правок в ТЗ v1.6 пока не требуют.
- 2026-09-26. По команде владельца из `Docs/` перенесены в `Docs/Корзина/` четыре документа: «MiniMem-1 - План правок ТЗ v1.6.md», «MiniMem-1_Reshenia_i_plan_pravok_v1.7Sonnet.md», «MiniMem-1_Решения_по_вопросам_и_план_правок_ТЗ_v1.6DS.md», «MiniMem-1_v1.7_Таблица_соответствия_вопросов.md». Причина: правки уже внесены в ТЗ (редакция v1.7), планы отработали. Каталог `Docs/Корзина/` не ведётся в репозитории, поэтому файлы удалены из индекса Git, копии остались на диске. Ссылка на план правок в заголовках ТЗ v1.6, плана реализации и changelog_tz.md сохранена как историческая: она указывает на исходный документ, а не на его текущее расположение.

## Известные расхождения

- 2026-09-25 · закрыто · patch set §1 против ТЗ v1.5 · потеря fence-обёрток. Нормативные блоки в §1 оформлены как text-fence, поэтому внутренние кодовые блоки были записаны простым текстом. По команде владельца от 2026-09-25 fence-обёртки возвращены в ТЗ v1.6: §4.4 (образец записи журнала, ```text), §9.1 (четыре блока ```sql + перечни значений meta и digest_status в ```text), §13 (пример блока памяти, ```text) и §13.1 (строки-делимитеры, fence с отступом внутри пункта 1), §14 (путь к файлу и образец дайджеста, ```text), §18 (три схемы жизненного цикла и параметры catch-up, ```text), §20.1 (перечень флагов, ```text), §21 (список команд, ```text), §22 (поля лога и значения operation, ```text). Нормативные списки (маркированные и нумерованные требования) намеренно оставлены обычным markdown. Подраздел 17.1 не добавляется по решению владельца.
- 2026-09-25 · контроль 6.3 · подраздел 17.1 в patch set отсутствует: в блоке пункта 1.29 подразделов 17.x нет. По решению владельца от 2026-09-25 17.1 не добавляется — раздел 17 в v1.6 заменён целиком и описывает состояние сессии. Расхождение закрыто.
- 2026-09-25 · patch set §1 · опечатка «записии» вместо «записи» встречается в блоках пунктов 1.14, 1.15, 137.2, 137.3. По решению владельца опечатка исправлена по смыслу при применении; в ТЗ v1.6 эти правки также исправлены по всему документу вместе с «залоировать» -> «залогировать», «некзакчающиеся строки» -> «изменённые строки, не совпадающие с оригинальными делимитерами», «поисковое term» -> «поисковый термин».
- 2026-09-25 · patch set §1 · подзаголовки, не содержащие номера (например «Первый ход сессии», «Каждый ход» в разделе 18 и имена команд CLI в разделе 21), в блоках записаны простым текстом; при варианте 2 разметка таких строк не восстанавливается.
- 2026-09-25 · закрыто · пункт 137.2, тест 50: опечатка «записии» признана опечаткой patch set по решению владельца, фрагмент найден по тексту ТЗ v1.5 («записи»), правка применена.
- 2026-09-25 · закрыто · пункт 1.15: граница раздела 4.4 восстановлена, образец записи журнала возвращён внутрь fence, раздел 4.5 стоит после полного 4.4.
- 2026-09-25 · закрыто · раздел 18.1 «Catch-up»: порядок заменён на «сначала уплотнение (mode_compaction = true), затем дайджест (mode_return = true)»; при потере состояния сессий после rebuild-index автоматический catch-up не выполняет ни уплотнение, ни создание дайджеста.
- 2026-09-25 · выполнено · §2 patch set: «Docs/MiniMem-1 - План реализации.md» заменён на версию v1.6 (269 -> 404 строки) по команде владельца от 2026-09-25. Прежняя редакция плана сохранена вне репозитория во временном каталоге.
- 2026-09-25 · закрыто · «Docs/changelog_tz.md»: файл создан, вводный блок истории правок вынесен из ТЗ v1.6, в ТЗ оставлена краткая ссылка.
- 2026-09-25 · устранено · тест 50 дополнен проверкой «количество терминов не превышает max_search_terms»; тест 3 — убраны лишние backtick вокруг `event_id`; новые тесты 51-115 получили явные номера MM-51…MM-115.
- 2026-09-26 · открыто · `tests/required_tests.txt` не соответствует редакции v1.7. Раздел 23 ТЗ v1.6 (в редакции v1.7) содержит 164 строки тестов, номера 1–164 без пропусков и повторов; из них MM-112 помечен «снят (дубль MM-96)». Раздел «Обязательные номера» задаёт состав: 1–50, 51–68, 79–89, 101–115 и все MM-116…MM-164, кроме снятых номеров; 69–78 и 90–100 имеют статус «действует, для первой приёмки не обязателен». Ожидаемый файл — 142 номера. Фактический файл содержал 115 номеров (1–115) и устарел: отсутствовали 116–164 (49 номеров), лишние 69–78, 90–100 (21 номер) и снятый 112.
- 2026-09-26 · закрыто · `tests/required_tests.txt` пересобран по §23 (редакция v1.7): 142 номера — 1–50, 51–68, 79–89, 101–164 без снятого MM-112. Формат: по одному номеру в строке, LF (CR-байтов 0), UTF-8 без BOM, пустых строк и дублей нет, диапазон 1…164. Номера сверены с §23: все присутствуют, лишних нет. Проверка соответствия описана в §23 и критериях приёмки (MM-153, критерии 11 и 25) — тест `tests/test_required_numbers.py` ещё не создан.
- 2026-09-26. По решению владельца документ переименован: `Docs/MiniMem-1 - Техническое задание v1.6.md` -> `Docs/MiniMem-1 - Техническое задание v1.7.md`. Причина: содержимое — редакция v1.7, а имя файла оставалось от предыдущей редакции, из-за чего адрес документа расходился с его содержимым. Правило: имя файла ТЗ совпадает с номером редакции внутри; при выпуске следующей редакции файл переименовывается, исторические упоминания старого имени в журнале решений и changelog остаются как есть. Обновлены рабочие ссылки: `Docs/MiniMem-1 - План реализации.md` (строка 3) и запись от 2026-09-25 в этом файле. Внутри ТЗ (строка 3) и в `Docs/changelog_tz.md` имя не дублируется — там только номер редакции.
- 2026-09-26. Выполнен этап 3 плана реализации (Дайджест). Созданы `minimem/mm/digest.py` и `minimem/hooks/session_end.py`; в `minimem/mm/store.py` добавлены выборки по сессии (`meta_by_session`, `meta_sessions`, `body_get`) и работа с таблицей `sessions` (`session_upsert`, `session_get`, `sessions_all`, `session_by_digest_file`); в `minimem/minimem.py` добавлена команда `rebuild-digest` (`--session`, `--all`, `--project`) и проверка дайджестов в `verify`. Тесты этапа — `minimem/tests/test_digest.py`, номера 31, 32, 33, MM-58, MM-92, MM-146, MM-147, MM-159, MM-164; прогон из `minimem/` — 146 тестов, код возврата 0. Номера уже присутствовали в `tests/required_tests.txt`, файл не изменялся.
- 2026-09-26. Приняты решения по этапу 3 (Дайджест), нормы ТЗ v1.7 не расширялись. 1. `max_digest_chars` и прочие лимиты §6 считаются в символах, а не в байтах: превышение проверяется по длине текста, пометка « [обрезано]» входит в лимит обрезки вместе с текстом. 2. Безопасная часть имени дайджеста не усекается по символу `_`: подчёркивания законны в Windows, убираются только точки и пробелы в хвосте (П-12, п.1-3). 3. Источник данных дайджеста — производный SQLite (`memory_meta` + `memory_fts`), а не журнал: это следует из MM-58 и §9.1; при сбое индекса дайджест не создаётся, состояние лечится `rebuild-index` и `rebuild-digest`. 4. Отсутствие `completed`/`failed`/`interrupted` в событии `on_session_end` трактуется как «Завершение: полное» — по MM-159. 5. Повреждённый или отсутствующий файл дайджеста — проблема отчёта `verify`, а не исключение: `read_header` возвращает `None`, файл перезаписывается пересборкой (§3.4). 6. Коллизия имён (один `digest_file` у нескольких `session_id`) блокирует перезапись: пишется `digest_name_collision`, `digest_status = failed`, содержимое чужого файла не меняется (П-12). 7. При нехватке места отбрасываются более ранние ходы, а не последние; ход, на который не хватает заголовка с метками «**Вы:**» и «**Агент:**», целиком не включается (§14). 8. При пустой сессии файл не создаётся, пишется `digest_empty_session`, `digest_status = none` (§14).
- 2026-09-26. Выполнен этап 4 плана реализации (Возврат). Созданы `minimem/mm/ret.py`, `minimem/mm/session.py`, `minimem/hooks/pre_llm.py`; в `minimem/mm/insert.py` добавлена `build_memory_block` (формат вставки §13 с санитизацией §13.1 и лимитом `max_return_chars`). Тесты этапа — `minimem/tests/test_ret.py`, номера 17, 18, 19, 20, 21, 30, 45, 46, MM-109, плюс MM-123, MM-124 и MM-125 в части кулдауна, лимита вставок и полей лога; прогон из `minimem/` — 168 тестов, код возврата 0. Все номера уже присутствовали в `tests/required_tests.txt`, файл не изменялся. Поиск по реплике (этап 5) не реализован: в хуке есть только gate по `mode_search` и лог `search_not_implemented`; catch-up (этап 7) и уплотнение (этап 6) не выполняются. Проверка на реальном журнале: проект `common`, 4 файла дайджеста, выбран дайджест сессии `2026-09-26-02` по максимальному «Последнему ходу» в UTC (2026-09-26T03:57:21+00:00), вставка 649 символов при лимите 1500 без усечения. Регистрация `pre_llm.py` в `%LOCALAPPDATA%\hermes\config.yaml` и в `shell-hooks-allowlist.json` не выполнена.
- 2026-09-26. Выполнен этап 5 плана реализации (Поиск по реплике). Созданы `minimem/mm/query.py` и `minimem/mm/search.py`; в `minimem/mm/store.py` добавлены `session_return_put` и `session_returned_ids` (таблица `session_returns` существовала с этапа 2 без методов); в `minimem/hooks/pre_llm.py` операция-заглушка `search_not_implemented` заменена реальным поиском, константа `SEARCH_PENDING` удалена; в `minimem/minimem.py` добавлена команда `search` с `--explain` и `--project` (§21.2, П-11). Тесты этапа — `minimem/tests/test_search.py`, 31 тест, номера 12, 22–29, 48, 49, 50, MM-81, MM-108; полный прогон из `minimem/` — 199 тестов (168 этапов 0–4 + 31 этапа 5), код возврата 0. Номера 81 и MM-81 входят в обязательный перечень §23, остальные номера этапа уже были в `tests/required_tests.txt` — файл не изменялся. Калибровка `search_score_threshold` на 20 реальных репликах накопленного журнала (8 записей, проект `common`): содержательные реплики дали top score 1.4443–4.8654, ложные совпадения — 0.8095, служебные («да», «ок», «ага», «угу», «спасибо», «давай», «продолжай», «хорошо», «принял») — ноль попаданий. Значение 1.0 подтверждено и зафиксировано в разделе 20 ТЗ. `mode_search` в рабочем `minimem/config.json` остаётся `false`; регистрация `pre_llm.py` в Hermes не выполнялась (этап 8).
- 2026-09-26. Решения по этапу 5 (Поиск), нормы ТЗ v1.7 не расширялись. 1. Список окончаний для усечения основ — константа `STEM_ENDINGS` в `mm/query.py`; окончание `ть` из него исключено, иначе «память» и «памяти» давали разные основы («памя» и «памят») и теряли совпадение (MM-48). 2. Удаление блока памяти из реплики перед построением запроса выполняется в хуке через `insert.strip_memory_block` (§10 п.3): иначе прошлая вставка искала бы саму себя. 3. Служебные реплики отсекаются единственным нормативным механизмом — порогом `search_min_word_len`; стоп-слов в ТЗ нет, и список служебных слов не вводился. 4. Порог применяется как `max(top * search_score_ratio, search_score_threshold)` (§12 п.8): относительный и абсолютный отсевы — два независимых ограничения, оба должны выполняться. 5. Причины отсева в `--explain` различают `below_threshold`, `already_returned`, `suppressed`, `same_session`, `other_project`, `no_body`, `no_date`, `over_limit`; записи без даты не вставляются ни при каком пороге (§13, §13.1 п.4). 6. Бюджет `max_return_chars` делится поровну между отобранными записями, а запись обрезается по строкам с пометкой «обрезано» в заголовке: обрезка целых записей выбрасывала бы релевантные записи из вставки, а обрезка блока оставляла бы обрезанный заголовок без даты. 7. `usage_count` и отметка в `session_returns` увеличиваются только для записей, фактически попавших в тело вставки (П-11). 8. Поиск не запускается на первом ходе и на ходе, где Возврат вставил (в том числе при сжатии контекста), — как требует §3.3. 9. `minimem search` не трогает индекс, счётчики и `session_returns`, но выполняет тот же пайплайн, включая двухступенчатый запрос; при печати чужие проекты видны как отсев `other_project` — для этого `plan()` получил параметр `restrict_project`, который хук всегда передаёт как `True`.
- 2026-09-26. Выполнен этап 6 плана реализации v1.7 (Уплотнение). Создан `minimem/mm/compaction.py`; в `minimem/mm/store.py` добавлены `meta_all`, `suppressed_map`, `suppressed_count`, `usage_all`; в `minimem/mm/indexer.py` `rebuild_index` получает параметр `config`, пишет ключ `rebuilt_at` в нормализованном UTC (§9.1) и вызывает `compaction.rebuild_suppression` (правила 1 и 3); в `minimem/minimem.py` добавлена команда `compact` с флагом `--dry-run`, а `cmd_rebuild_index` передаёт `config=config`. Тесты этапа — `minimem/tests/test_compaction.py`, 22 теста, номера 34–39, 43, 44, MM-54, MM-60, MM-64, MM-65, MM-79; полный прогон из `minimem/` — 221 тест (199 этапов 0–5 + 22 этапа 6), код возврата 0. Все номера уже присутствовали в `tests/required_tests.txt` — файл не изменялся. Проверка на копии рабочего хранилища (8 записей, проект `common`, каталог `C:\Temp\mm-compact-trial`, копия удалена после прогона): `verify` — «без проблем» (код 0), `rebuild-index` — проиндексировано 8 (код 0), `compact` — снято 0 (по возрасту 0, дублей 0, неиспользованных 0), повторный `compact` — те же числа и код 0, `verify` после уплотнения — «без проблем». Ноль снятых ожидаем: журнал накоплен за один день, а `compaction_age = 180`. Рабочее хранилище не изменялось. `mode_compaction` и `compaction_rule2_enabled` в рабочем `minimem/config.json` остаются `false`; вызов уплотнения из `on_session_end` — этап 7, регистрация хуков — этап 8.
- 2026-09-26. Решения по этапу 6 (Уплотнение), нормы ТЗ v1.7 не расширялись. 1. Модуль уплотнения читает только производный слой SQLite (`memory_meta` + `usage_counters`), журнал не открывается: это следует из §15 (правила 1 и 3 определяются журналом, но журнал — источник истины, а не рабочий слой уплотнения) и из границы этапа, где пересборкой индекса занимается `indexer`. 2. Причина снятия пишется в `suppressed_records.reason` значениями `age`, `duplicate`, `unused`; при совпадении нескольких правил выбирается причина по порядку 1 -> 3 -> 2 (`REASON_PRIORITY`). Результат от приоритета не зависит — запись снимается при совпадении хотя бы одного правила, — но служебный слой становится воспроизводимым по одному правилу. 3. Уже снятая запись не переписывается: `suppress` обновляет только `reason`, а `suppressed_at` остаётся от первого применения, поэтому повторный прогон идемпотентен (тест 37). 4. Tie-breaker §15 реализован сравнением через `functools.cmp_to_key`, а не кортежем: норма сравнивает `turn_numeric` только когда он есть у обеих записей, а иначе переходит на `turn` как строку, и кортежный ключ такую условность выразить не может. Проверено: сравнение антисимметрично на 132 парах, порядок группы не зависит от порядка записей на входе (тесты MM-60), а производные ходы (`turn` = «?», `turn_numeric IS NULL`) оказываются в конце группы, поскольку «?» больше любой цифровой строки. 5. `rebuilt_at` пишется в нормализованном UTC (тем же способом, что `timestamp_utc` в `memory_meta`), иначе строковое сравнение `timestamp_utc > rebuilt_at` в правиле 2 было бы некорректным для не-UTC смещений. 6. Пересчёт снятых после `rebuild-index` делает сам `indexer.rebuild_index`, а не CLI: §21.5 относит восстановление к `rebuild-index`, и только так правило 2 исключается автоматически. 7. `rebuild_index` при `config = None` только пересобирает индекс — параметр необязательный, чтобы вызовы без конфигурации (восстановление из §4.3) не зависели от настроек уплотнения. 8. `compact` — административная команда: она выполняется при `mode_compaction = false` (§3.5, §3.7.7), но уважает `compaction_rule2_enabled` и не включает правило 2 сама (§15.1, MM-79). Флаг `--dry-run` показывает план снятия, не записывая его; добавлен как безопасная административная возможность, не меняющая нормы. 9. Gate автоматического уплотнения вынесен в `auto_compaction_enabled` и `note_disabled`: при `mode_compaction = false` состояние снятия не меняется, в лог пишется `compaction_skipped` с `error_detail_code = mode_compaction_disabled` (§22, MM-54). Сам вызов из `on_session_end` — этап 7. 10. Новые операции лога — `compaction_applied`, `compaction_rebuilt`, `compaction_skipped`: перечень операций в §22 открыт формулировкой «в том числе», а состав полей §22 и §19.5 расширен не был — используются существующие `records_scanned`, `records_suppressed`, `duration_ms`, `error_detail_code`, плюс счётчики причин в отдельном поле `suppression_by_rule`. 11. `mode_compaction` в рабочем `minimem/config.json` не включался без команды владельца.
- 2026-09-26. Выполнен этап 7 плана реализации v1.7 (Жизненный цикл, сессии, catch-up, надёжность). Создан `minimem/mm/catchup.py`; в `minimem/mm/config.py` добавлены `catch_up_budget_ms` и `digest_budget_ms`; в `minimem/mm/session.py` добавлены `save_metrics` и внутренняя `_write_session`, `register_turn` пишет метрики в `sessions`; в `minimem/mm/digest.py` `build_digest` принимает `deadline_remaining_ms`; в `minimem/mm/ret.py` добавлена проверка суббюджета 30 % при построении дайджеста на сжатии; в `minimem/hooks/pre_llm.py` добавлен шаг catch-up; в `minimem/hooks/session_end.py` добавлены `run_compaction_step` и `mark_completed`, порядок «уплотнение -> дайджест». Тесты этапа — `minimem/tests/test_stage7.py`, 34 теста, номера 40, 41, MM-52, MM-55, MM-74, MM-75, MM-76, MM-77, MM-78, MM-110, MM-111, плюс MM-113 и П-06; полный прогон из `minimem/` — 255 тестов, код возврата 0. Все номера уже присутствовали в `tests/required_tests.txt` — файл не изменялся. `scripts/` — 23 теста, код 0; `validate_index.py --strict` — PASS, код 0; `build_refs_registry.py --check` — «Реестр ссылок актуален», код 0. `mode_compaction` и `compaction_rule2_enabled` в рабочем `minimem/config.json` остаются `false`; регистрация хуков в Hermes — этап 8. Рабочее хранилище и файлы вне репозитория не изменялись, Hermes не запускался.
- 2026-09-26. Проверка этапа 7 на копии рабочего хранилища (каталог `C:\Temp\mm-stage7-trial`, копия `minimem.db` и каталога `common`, копия удалена после прогона; рабочее хранилище на запись не открывалось, Hermes не запускался). Конфигурация копии — `memory_root` во временном каталоге, `project_mapping` пуст, `mode_return = true`, `mode_compaction = true`. Результаты. 1. Выбор предыдущей сессии на реальных данных корректен: кандидат `2026-09-26-02` с максимальным нормативным «Последним ходом» `2026-09-26T03:57:21+00:00` из четырёх дайджестов — тот же файл, который затем прочитает Возврат (§11.1, П-07). Это проверяло единственное место, недоступное тестам: в рабочем хранилище четыре разных формата `session_id` (`2026-09-26-01`, `2026-09-26-02`, `20260926_032533_bd0776`, `20260926_035224_bc9d07`) и метки `last_seen_at` в двух часовых поясах, тогда как тесты используют один формат и один пояс. Строковое сравнение ISO-дат отработало верно. 2. catch-up отработал: `status = ok`, `target = 2026-09-26-02`, `attempts = 0`, `reason = digest_up_to_date`, `deadline_remaining_ms = 12000`; уплотнение выполнилось (`compaction_applied`, просмотрено 8 записей, 0 новых снятых — записи моложе `compaction_age = 180`); состояние текущей сессии записано (`pending_catch_up = 0`, `catch_up_attempts = 0`, `catch_up_target_session_id` задан); повтор на втором ходе отклонён с `reason = not_pending`. 3. `verify` по копии после catch-up — «без проблем», код 0: 8 записей, 8 единиц FTS, 4 дайджеста, максимум 1500 символов, коллизий и отсутствующих файлов нет. Расхождений между состоянием, записанным catch-up, и независимой проверкой не выявлено. 4. Найден реальный сценарий на копии: сессия `2026-09-27-01` присутствует в `sessions`, но не имеет ни одной записи в индексе; если кандидатов по §11.1 нет, fallback выбирает её и `build_digest` возвращает `empty`. Поведение отделано правильно — `status = failed`, `reason = digest_failed`, `pending_catch_up = 1`, `catch_up_attempts = 1`, далее `attempts = 2` и `catch_up_attempts_exhausted`: лимит повторов соблюдается, нехватка данных не приводит к бесконечным попыткам. 5. Подтверждено наблюдение из пункта 3 разбора: при ручном удалении файлов дайджестов `verify` сообщает «дайджесты, отсутствующие при `digest_status = done`: 4» и возвращает код 1, то есть расхождение он видит, но висящий `pending_catch_up` не показывает — проверки этого поля в §21.5 нет, и этап 7 её не добавлял. Замечания по инструментам: у `minimem.py` ключ `--config` передаётся до имени команды (`minimem --config <путь> verify`), переменная `MINIMEM_CONFIG` CLI не читает — её поддерживают только скрипты хуков; ошибки в ходе проверки возникали в диагностических скриптах, не в коде MiniMem.
- 2026-09-26. Решения по этапу 7 (Жизненный цикл, сессии, catch-up, надёжность), нормы ТЗ v1.7 не расширялись. 1. Catch-up вынесен в отдельный модуль `mm/catchup.py`, а не реализован в хуке: §26.2 требует, чтобы вся логика жила в модулях, и у хука остаётся тонкий контур (§19.2). 2. Кандидат на предыдущую сессию выбирается переиспользованием `ret.select_last_digest` (§11.1, П-07), а не второй реализацией правила выбора: так catch-up строит дайджест ровно той сессии, которую затем прочитает Возврат. Откат — последняя сессия проекта по `last_seen_at DESC, session_id DESC` (тест MM-74). 3. Расчёт бюджетов вынесен в `config.catch_up_budget_ms` и `config.digest_budget_ms`, а не оставлен в `catchup`: `ret` тоже обязан знать суббюджет дайджеста (MM-111), а импорт `ret -> catchup` образовал бы цикл, поскольку `catchup` импортирует `ret`. 4. `catch_up_budget_seconds` трактуется как верхняя граница: берётся минимум из 40 % `hook_deadline_ms` и `catch_up_budget_seconds` (§19.1.1, §20). 5. Нехватка бюджета откладывает catch-up (`pending_catch_up = 1`, `catch_up_attempts` увеличивается, пишется `catch_up_timeout`), но не обрывает по лимиту времени; Возврат выполняется по имеющимся данным. Отказ шага уплотнения при уже построенном дайджесте оставляет `pending_catch_up = 1` (повтор разрешён), отказ дайджеста засчитывает попытку. 6. Потеря состояния сессий после `rebuild-index` определяется так: в проекте есть записи индекса (`meta_sessions`), но в `sessions` нет ни одной чужой строки. Проверка выполняется до выбора кандидата, иначе потеря состояния выглядела бы как «предыдущей сессии нет». Журнал при этом не сканируется (§18.1 п.4), уплотнение и дайджест не создаются, а Возврат читает только существующие валидные дайджесты. 7. Поле «Завершение» дайджеста в catch-up выводится из `sessions.completed`: `completed = 0` даёт «прервано» (§18.1 п.6.1, П-17); правила уплотнения при этом выполняются как обычно. 8. `sessions.max_history_len` заполняется значением `max_chars` — длина истории без блоков памяти в символах, как измеряет `HistoryMetrics` (§17, §11); `max_msgs` и `max_chars` пишутся рядом, схема §9.1 не менялась. 9. `sessions.completed` пишет хук `on_session_end` по §26.2: `completed = true` даёт 1, `interrupted` или `failed` дают 0, отсутствие полей даёт 0 и не мешает созданию дайджеста (MM-159). 10. Шаг catch-up в `pre_llm.py` обёрнут в `try/except` отдельно от границы хука: отказ catch-up не должен отменять Возврат, который идёт следом и не зависит от него (§18, §19.2). 11. Шаг уплотнения в `session_end.py` также изолирован от шага дайджеста: дайджест создаётся и при недоступном уплотнении, а хук в любом случае печатает `{}` и завершается кодом 0. 12. Ранний выход `pre_llm.py` срабатывает только когда выключены все три режима (`mode_return`, `mode_search`, `mode_compaction`): раньше условие не учитывало `mode_compaction`, и catch-up не мог бы отработать при выключенном Возврате (§18.1, MM-78). 13. Новые операции лога ограничены перечнем §22: `catch_up`, `catch_up_timeout`, `catch_up_state_unavailable` плюс `return_digest_timeout` для таймаута дайджеста при сжатии (MM-111) — этот случай §19.1.1 требует логировать, но отдельной операции в перечне не имеет. Состав полей §19.5 и §22 не расширялся; добавлены только поля `catch_up_target_session_id` и `catch_up_attempts` как конкретизация существующего `error_detail_code`. 14. `mode_compaction` и `compaction_rule2_enabled` в рабочем `minimem/config.json` без команды владельца не включались; catch-up проверялся на временных конфигурациях тестов, рабочее хранилище не изменялось.
- 2026-09-26. Решения по этапу 4 (Возврат), нормы ТЗ v1.7 не расширялись. 1. Счётчики сессии для детекции сжатия (точки отсчёта `max_msgs`/`max_chars`, счётчик ходов, ход последнего сжатия, число вставок Возврата) хранятся в таблице `meta` под ключом `session_state:<project>/<session_id>`, а не в `sessions`: схема `sessions` из §9.1 этих полей не содержит, а менять её значило бы менять нормативную схему. Прецедент служебных ключей в `meta` уже есть (`damaged:`, П-13). 2. Сообщение истории, опустевшее после удаления блока памяти, в измерение `hist_msgs`/`hist_chars` не входит — это прямо следует из §17 («может исключаться из измерения, если платформа передаёт память отдельным сообщением») и нужно, чтобы MM-109 не зависел от способа доставки памяти. 3. Единицы измерения: `hist_msgs` считает сообщения, `hist_chars` — символы; оба порога применяются по формуле §11 без изменений. 4. `threshold_applied` пишется строкой сработавших условий (например `hist_msgs<20*0.6 drop>=6`), при отсутствии сжатия — `none`. 5. Лимит вставок проверяется в Возврате, а не в Дайджесте: при его исчерпании пишется `return_injection_limit` и ход продолжается без вставки. 6. Если бюджет не позволяет даже делимитеров с пометкой обрезки, блок не вставляется вовсе — пустой блок памяти хуже его отсутствия. 7. Поиск на первом ходе и на ходе Возврата не запускается (§12 п.2); при `mode_search` на остальных ходах пишется `search_not_implemented` до появления этапа 5. 8. Текст дайджеста вставляется как есть (после санитизации), дописывается только предупреждение о прерванной сессии (§14, П-17).
- 2026-09-26. По команде владельца (ограничение «строго не выходить за пределы этапа» снято) зарегистрированы два оставшихся хука Hermes, досрочно закрыт незавершённый шаг установки. Резервные копии вне репозитория: `config.yaml.minimem-20260926-165245.bak` и `shell-hooks-allowlist.json.minimem-20260926-165245.bak`. В `config.yaml` в блок `hooks` добавлены `pre_llm_call` (таймаут 15) и `on_session_end` (таймаут 30) с командами `hermes-agent\venv\Scripts\python.exe` + `C:\Projects\MEM_Hermes\minimem\hooks\<скрипт>.py`; в `shell-hooks-allowlist.json` в массив `approvals` добавлены две пары `event`+`command`, существующие записи сохранены. `hooks_auto_accept` остался `false`: первый запуск хуков Hermes попросит подтверждение в терминале. Проверка: `hermes hooks list` — 4 записи, все MiniMem allowlisted (код 0); `hermes hooks doctor` — «All shell hooks look healthy», все четыре хука прошли синтетическую полезную нагрузку (код 0); `minimem doctor` впервые отдал «итог: без проблем» (код 0) — раньше он сообщал `config_invalid` о нерегистрированном `on_session_end`. Тест 47 выполнен реально с `MINIMEM_RUN_HERMES_TESTS=1`: 39 тестов этапа, OK, пропусков нет (код 0). Полный прогон из `minimem/` — 294 теста, OK, 3 пропуска (код 0); `scripts/` — 23 теста, код 0; `validate_index.py --strict` — PASS, код 0; `build_refs_registry.py --check` — «Реестр ссылок актуален», код 0. Найденная и исправленная при этом ошибка: разбор `config.yaml` в `mm/doctor.py` при нескольких записях одного события (`pre_llm_call` теперь содержит хук MiniMem и сторонний `wiki_hook.py`) перетирал команду предыдущей записи, из-за чего `doctor` не видел `pre_llm.py`; разбор разделяет записи по элементам `- command:`. Рабочее хранилище не изменялось.
- 2026-09-26. Выполнен код этапа 8 плана реализации v1.7 (Установка и приёмка). Созданы `minimem/mm/doctor.py` и `minimem/mm/stats.py`; в `minimem/minimem.py` добавлены команды `doctor` и `stats`; тесты этапа — `minimem/tests/test_stage8.py`, 39 тестов. Полный прогон из `minimem/` — 294 теста (255 этапов 0–7 + 39 этапа 8), код 0; `scripts/` — 23 теста, код 0; `validate_index.py --strict` — PASS, код 0; `build_refs_registry.py --check` — «Реестр ссылок актуален», код 0. `tests/required_tests.txt` не изменялся: номера 47 и MM-165…MM-168 уже присутствовали. Правки вне репозитория не выполнялись: регистрация `pre_llm.py` и `session_end.py` в Hermes и rollout по §26.4 ждут команды владельца.
- 2026-09-26. Решения по этапу 8, нормы ТЗ v1.7 не расширялись. 1. `minimem stats` (П-10) реализован в этом этапе, хотя в списке «что делаем» этапа 8 он не значится: §25 (критерий 28) требует непустой отчёт `stats` за день эксплуатации, номера MM-140, MM-141, MM-142 входят в обязательные, и ни один другой этап их не закрывает. Обоснование зафиксировано здесь, а не добавлением пункта в план. 2. `config.yaml` Hermes разбирается собственным построчным разбором блока `hooks` (только стандартная библиотека, как требует §26.2 для хуков, и как требует §26 для установки): PyYAML во встроенном Python Hermes нет, а ставить зависимость ради чтения трёх ключей означало бы усложнить установку. Разбор понимает форму §26.1 (`hooks:` верхнего уровня, событие, элементы `- command:` и `timeout:`) и закрывается на любом ключе уровня блока или выше — иначе `code_execution.timeout` был бы принят за предел хука (учтено тестом). 3. `hook_deadline_ms` в `doctor` считается по формуле §19.1.1 как `min(таймаут Hermes по config.yaml, внутренний предел) − hook_safety_margin_ms`; `on_session_end` сверяется с внутренним пределом `post_llm` (30 с), потому что §26.1 задаёт для него тот же предел, а отдельного ключа конфигурации у него нет. 4. Отсутствие записи события в `config.yaml` — расхождение `config_invalid`, а не «пропущенная проверка»: §21.1 требует сверять таймауты хуков Hermes с конфигом MiniMem, и нерегистрированный хук и есть расхождение. Именно так сейчас выглядит рабочая система: `on_session_end` не зарегистрирован. 5. Коды `config_invalid`, `budget_mismatch`, `backup_stale` (§21.1) — это коды расхождений в отчёте и в логе; код возврата CLI остаётся общим: 0 при отсутствии расхождений, 1 при их наличии, 2 при нечитаемой конфигурации. 6. Суммы суббюджетов в отчёте приведены по каждому хуку отдельно: `pre_llm_call` — catch-up 40 % + дайджест при сжатии 30 % + `return_min_budget_ms`, `post_llm_call` — захват 40 % + индексатор 40 % (§19.1.1, таблица долей). `catch_up_budget_ms` берётся из `config`, где уже учтена верхняя граница `catch_up_budget_seconds`, чтобы отчёт и хук считали одно и то же. 7. Путь к `backup.json` не выводится из конфигурации MiniMem: §21.1 прямо говорит, что этот файл в §20 не входит. Поиск: переменная окружения `MINIMEM_BACKUP_SETTINGS`, затем запасной каталог `minimem-backup` рядом с `memory_root` (раздел 3 документа «Установка, бэкап и восстановление»); в текущей системе `backup.json` лежит в `D:\minimem-backup`, поэтому проверка запускается с `--backup-settings`. 8. Ключа `backup_stale_days` в `D:\minimem-backup\backup.json` нет; при его отсутствии применяется порог по умолчанию 2 сут из предложения П-19 (раздел 4) и это печатается в отчёте строкой «порог … (по умолчанию …, ключ backup_stale_days в backup.json не задан)». Порог принадлежит эксплуатации; решение владельца — добавить ключ явно. 9. Отсутствие файла настроек и отсутствие ключа `backup_root` дают решение «пропущена» с пометкой о том, что копирование вне области MiniMem, и ошибкой не считаются (§21.1, MM-168). Непригодной копия считается по превышению порога либо когда в слепке нет ни журнала, ни `config.json`; наличие только одного из двух прерванным копированием не считается. 10. Слепок выбирается по имени каталога `<YYYY-MM-DD>` (максимальная дата), а не по времени изменения: §21.1 говорит об имени последнего каталога копии. 11. `doctor` не обращается к SQLite и к журналу: обе проверки — таймауты и копия — от индекса не зависят (§21.1). Файлов он не меняет ни в `%LOCALAPPDATA%\hermes`, ни в `backup.json`, ни в рабочем хранилище; в лог MiniMem пишется только строка результата `doctor` (§22). 12. В `stats` «нет попаданий» и «ошибка/таймаут» — разделы отчёта, а таймаут поиска учитывается и как отказ, и как таймаут: §19.5 требует различать «нет попаданий» и «ошибка/таймаут», то есть две категории, а не три. 13. Перцентили задержек считаются по ближайшему рангу: §21.3 задаёт p50/p95/max, но не задаёт метод интерполяции, а для эксплуатационного отчёта различие незаметно. 14. Тест 47 (установка) реализован как `InstallationTest47` с пропуском без переменной `MINIMEM_RUN_HERMES_TESTS=1`: он запускает настоящий Hermes и выполняется только по команде владельца, поэтому в обычном прогоне даёт пропуск, а не фиктивный успех. 15. MM-163 и MM-164 к `doctor` не относятся — это П-02 (несколько ревизий `event_id` в `verify` и выбор последней ревизии в дайджесте); оба номера реализованы и проверены на этапах 2 и 3, повторная реализация в этапе 8 не требовалась. 16. Рабочее хранилище не изменялось, режимы в `minimem/config.json` не переключались; проверки `doctor` и `stats` выполнены на рабочей системе только на чтение.

 для детекции сжатия (точки отсчёта `max_msgs`/`max_chars`, счётчик ходов, ход последнего сжатия, число вставок Возврата) хранятся в таблице `meta` под ключом `session_state:<project>/<session_id>`, а не в `sessions`: схема `sessions` из §9.1 этих полей не содержит, а менять её значило бы менять нормативную схему. Прецедент служебных ключей в `meta` уже есть (`damaged:`, П-13). 2. Сообщение истории, опустевшее после удаления блока памяти, в измерение `hist_msgs`/`hist_chars` не входит — это прямо следует из §17 («может исключаться из измерения, если платформа передаёт память отдельным сообщением») и нужно, чтобы MM-109 не зависел от способа доставки памяти. 3. Единицы измерения: `hist_msgs` считает сообщения, `hist_chars` — символы; оба порога применяются по формуле §11 без изменений. 4. `threshold_applied` пишется строкой сработавших условий (например `hist_msgs<20*0.6 drop>=6`), при отсутствии сжатия — `none`. 5. Лимит вставок проверяется в Возврате, а не в Дайджесте: при его исчерпании пишется `return_injection_limit` и ход продолжается без вставки. 6. Если бюджет не позволяет даже делимитеров с пометкой обрезки, блок не вставляется вовсе — пустой блок памяти хуже его отсутствия. 7. Поиск на первом ходе и на ходе Возврата не запускается (§12 п.2); при `mode_search` на остальных ходах пишется `search_not_implemented` до появления этапа 5. 8. Текст дайджеста вставляется как есть (после санитизации), дописывается только предупреждение о прерванной сессии (§14, П-17).
