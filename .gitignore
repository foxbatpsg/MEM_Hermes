# Архив и фон — не индексируются и не ведутся в репозитории
Docs/Корзина/
Docs/_фон/

# Промпт на следующую сессию — рабочий файл сессии, не результат этапа
промпт на следующую сессию.md

# Python
__pycache__/
*.pyc

# Генерируемые реестры (REFS-REGISTRY.md/.json, CARDS-REGISTRY.*) намеренно
# остаются в репозитории: их актуальность проверяет scripts/validate_index.py
