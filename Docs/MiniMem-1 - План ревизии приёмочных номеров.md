# MiniMem-1 — План ревизии приёмочных номеров

Статус: **рабочий материал, создан 2026-09-26 по команде владельца.** В нормативный
корпус не входит: это не спецификация, а разбор разрыва между планом разработки и
приёмочными критериями ТЗ. Нормы здесь не меняются.

Основание: ТЗ v1.7 §23 (приёмочные тесты, обязательные номера), §25 (критерии
готовности, п. 24 и 25), §21.4 (`verify --projects` и `re-project`, П-16), §4.4
п. 3 (`fmt`), §13.1 (нейтрализация вставки), §7.1 (`redaction`), §9.2
(целостность индекса), §18.1 (catch-up), П-18 (`sender_id`).

## 1. Что произошло

Этапы 0–8 плана реализации выполнены, приёмка по §26.4 проведена, система
работает. Но ревизия показала: **146 обязательных номеров в
`tests/required_tests.txt` не равны 146 выполненным проверкам.** 38 номеров не
упомянуты ни в одном тесте. Часть из них — просто номер не вписан в docstring
(проверка фактически есть), часть — реальные пробелы в коде.

Метод ревизии: сплошной поиск номеров в `minimem/tests/*.py`, затем точечный
поиск по коду `minimem/mm/` и `minimem/hooks/` для каждого из 38 номеров. Ниже
классификация по факту чтения кода, а не по предположению.

## 2. Категории

- **Закрыт фактически** — проверка в тестах есть, номер не указан в docstring.
  Действий не требуется, кроме вписания номера.
- **Требует проверки** — по коду судить нельзя однозначно; нужен взгляд на конкретный
  тест или пробный прогон. Это самая большая категория.
- **Не реализовано** — в коде отсутствует однозначно, найдено поиском по коду.

## 3. Ревизия по номерам

### 3.1. Не реализовано (8 номеров)

| Номер | Требование §23 | Факт в коде | Что нужно |
|---|---|---|---|
| 120 | `fmt` больше поддерживаемого индексируется, `verify` выдаёт `record_format_newer` | `record_format_newer` в коде отсутствует; `fmt` есть в `canonical.py` и `journal.py`, но сравнения с поддерживаемой версией нет | проверка версии в индексаторе + предупреждение в `verify` + тест |


### 3.2. Закрыт фактически (11 номеров)

Номер не вписан в docstring, проверка существует:

| Номер | Где закрыт |
|---|---|
| 3 | `test_capture.py`: `test_101_event_id_stable_on_redelivery`, `test_7_identical_turns_get_distinct_event_ids_same_hash` |
| 42 | `test_indexer.py`: `test_11_rebuild_after_deleting_db_restores_index` (закрывает отсутствие SQLite; повреждение — см. п. 3.3) |
| 47 | `test_stage8.py`: `InstallationTest47`, выполнен реально 2026-09-26 |
| 53 | `test_search.py`: `test_mode_search_false_inserts_nothing` плюс режимы в `test_search.py` |
| 80 | `test_compaction.py`: `test_rule2_requires_record_created_after_rebuild` |
| 119 | `test_capture.py`: `test_100_unknown_metadata_key_is_not_damage` (неизвестный ключ не теряет текст) |
| 121 | `mm/canonical.py`: `FMT` трактуется по умолчанию; поведение покрыто тестами чтения записей |
| 122 | `test_ret.py`: тесты сжатия по `hist_msgs` / `hist_chars` |
| 125 | `mm/session.py`: `compaction_decision` пишется в лог; тесты этапа 7 |
| 131 | `test_stage8.py`: `test_missing_hook_registration_is_reported` (doctor сообщает о расхождении таймаутов) |
| 143 | `test_search.py`: `test_query_modes_use_expected_operator`, `test_relaxation_to_or_keeps_terms_and_logs_mode` |

### 3.3. Требует проверки (19 номеров)

Здесь я не могу сказать «есть» или «нет», не написав тест. Это честная граница
ревизии: подтвердить или опровергнуть — работа этапа 1 плана.

| Номер | Что именно предстоит проверить |
|---|---|
| 56 | режим «Накопление» пишет журнал, но не влияет на контекст Hermes |
| 82 | запись, содержащая `=== КОНЕЦ ДАННЫХ ПАМЯТИ ===`, не ломает границу вставки |
| 83 | управляющие Unicode-символы нейтрализуются перед вставкой |
| 84 | запись, содержащая `### User` / `### Assistant`, обрабатывается безопасно |
| 87 | JWT заменяется placeholder'ом (шаблон в коде есть, отдельного теста не найдено) |
| 119 | чтение записи с неизвестным ключом целиком — текст не теряется (частично закрыто) |
| 126 | дайджест с текстом, содержащим делимитер, не создаёт вторую границу |
| 127 | измерение истории не искажается дайджестом с sanitized-делимитером |
| 128 | catch-up не стартует, если остаток дедлайна меньше его бюджета |
| 129 | Возврат выполняется, даже если catch-up прерван по бюджету |
| 130 | конфигурация отклоняет набор, где сумма суббюджетов превышает deadline |
| 132 | после `rebuild-digest` старой сессии Возврат не выбирает её дайджест как последний |
| 133 | catch-up строит дайджест именно той сессии, которую выберет Возврат |
| 144 | относительный отсев применяется относительно лучшего score текущей выдачи |
| 145 | `search --explain` печатает score и причину отсева, не меняя индекс и счётчики |
| 151 | `config_hash` меняется при правке поведенческого параметра и не меняет журнал |
| 152 | нарушение инварианта даёт `config_invalid` и безопасные значения по умолчанию |
| 155 | `project_path` пишется и нормализуется по правилам §8 |
| 160 | `sender_id` и `platform` сохраняются в записи журнала и не влияют на поиск |

## 4. Отдельно: `sender_changed` (MM-161)

`§26.1` и §22 требуют операцию `sender_changed`: при непустом `sender_id`,
отличающемся от предыдущего хода, пишется событие в лог. `sender_id` в записи
журнала сохраняется (проверено: `mm/capture.py` передаёт `sender_id` и
`platform` в `TurnData`), но самого события в коде нет — поиск по `mm/` и


## 6. Порядок работ

Этап 1 — проверка (без правок кода). Для каждого из 19 номеров категории 3.3
написать пробный тест и получить один из трёх фактов: «есть, покрыто»,
«проверки нет, но поведение верное» (тогда вписать номер), «поведение неверное»
(тогда номер переходит в этап 2). Результат — обновлённая таблица 3.3.

Этап 2 — реализация 8 номеров категории 3.1 плюс `sender_changed` по решению
владельца. Для каждого: код, тест с номером в docstring, запись в INDEX.md.

Этап 3 — инфраструктура приёмки (п. 5) и прогон всех номеров; отчёт
`tests/acceptance_v1.6.md` собирается последним, когда статусы известны.

Порядок внутри этапа 2 предлагаю: сначала MM-150, MM-120, MM-139 (целостность и
совместимость записей), затем MM-156 и MM-157 (привязка проекта), затем MM-88
(redaction), затем инфраструктура. Обоснование: первые три защищают данные от
тихой потери, MM-156/157 — восстановление при неверной привязке.

## 7. Чего этот план не делает

- Не меняет нормы ТЗ: ни один пункт не требует правки §23 или §25.
- Не трогает `tests/required_tests.txt`: состав обязательных номеров задан
  перечнем §23 и не пересматривается.
- Не отмечает снятыми те номера, которые поведением не подтверждены: снятие
  допускается только по §23 и только с указанием причины.
- Не выполняет саму проверку категории 3.3 — это этап 1 следующей сессии.

`hooks/` не даёт ни одного вхождения. Это не попало в таблицы выше, потому что
MM-161 относится к П-18, а не к «чистой» приёмке: требуется решение владельца,
считать ли это дефектом (событие не записано) или сознательным упрощением.

## 5. Инфраструктура приёмки (критерии §25 п. 24 и 25)

Требуются два файла, которых нет:

1. `tests/acceptance_v1.6.md` — отчёт о прогоне со всеми номерами 1…N и
   статусами, включая снятый MM-112. Критерий §25 п. 24: отсутствие номера в
   отчёте равносильно непройденной приёмке. Содержание не выдумывается: номера
   берутся из `tests/required_tests.txt`, статусы — из фактического прогона.
2. `tests/test_required_numbers.py` — тест, падающий, если в отчёте нет ни
   одного номера из `required_tests.txt` (MM-153) и проверяющий, что отчёт
   содержит номера 1…N (MM-154).

| 139 | `verify` перечисляет записи с производными ключами | `turn_source` встречается в индексаторе 2 раза, но отдельного перечисления в отчёте `verify` нет | вывод списка записей с `turn_source=derived` в `verify` + тест |
| 150 | внешнее переписывание файла журнала обнаруживается как `journal_externally_modified` | операция отсутствует полностью | детекция расхождения размера/mtime с курсором + событие + тест |
| 156 | `verify --projects` сообщает о записях с расхождением маппинга | флаг ограничивает проверку текущим проектом; самой сверки `project_path` записей с нормализованным путём нет | сверка в `verify --projects` + тест |
| 157 | `re-project` применяет исправленную привязку с резервной копией журнала | команда отсутствует; в `minimem.py` есть только упоминание в списке CLI | команда `re-project [--backup]` по §21.4 + тест |
| 153 | проверка обязательных номеров падает, если в отчёте нет ни одного номера из `required_tests.txt` | файла `tests/test_required_numbers.py` нет | тест (п. 5) |
| 154 | отчёт приёмки содержит все номера 1…N, включая снятые и пропуски | файла `tests/acceptance_v1.6.md` нет | генерация отчёта (п. 5) |
| 88 | PEM-ключ заменяется placeholder'ом | в `mm/redaction.py` есть JWT-шаблон (`eyJ`), PEM-шаблона нет | добавить шаблон в перечень §7.1 + тест |
